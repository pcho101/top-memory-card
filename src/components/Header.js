import React, { Component } from "react";

class Header extends Component {
  render() {
    return (
      <header>
        <h1>Cardcaptor Sakura Memory Game</h1>
        <p>There are 4 rounds. Try to capture each card only once in each round. Can you reach the end?</p>
      </header>
    )
  }
}

export default Header;
